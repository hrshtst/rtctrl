[project]
name = "rtctrl-docs"
version = "0.1.0"
description = "Documentation site tooling for rtctrl (MkDocs + Material)"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material>=9.6,<10",
]

[tool.uv]
package = false
