{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */tools/*.py) uvx ruff check \"$f\" 2>/dev/null;; esac",
            "timeout": 30,
            "statusMessage": "ruff check"
          }
        ]
      }
    ]
  }
}
